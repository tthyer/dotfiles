# Jira Preferences

- The user mostly works in the Jira project Data Platform (`DP`). When creating Jira tickets and no project is specified, prefer Data Platform.
- When the user asks to create a Jira issue, they mean actually create it with `acli`, not just draft copy-ready text.
